//autoexec
//This is just what triggers the other cfg files of the same names, don't change anything here
//Rather you should edit the files listed below, just look for the one under the same name with .cfg at the end

exec gfx
exec binds
exec settings
exec network
exec custom


